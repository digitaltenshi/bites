
<!-- CATEGORY FILTER RESULTS ex: http://base-template.squarespace.com/blog/?category=category1 -->

{.section categoryFilter}
  <h4 class="filter--text">Filtering by Category: {@|safe}</h4>
{.end}

<!-- TAG FILTER RESULTS ex: http://base-template.squarespace.com/blog/?tag=tag2 -->

{.section tagFilter}
  <h4 class="filter--text">Filtering by Tag: {@|safe}</h4>
{.end}

<!-- AUTHOR FILTER RESULTS ex: http://www.base-template.com/blog?author=4f046df4d09ae2cf23d1aaa2 -->

{.if authorFilter}
  <h4 class="filter--text">Filtering by Author: {author.displayName}</h4>
{.end}

<!--ITEM LOOP-->

{.repeated section items}

  <!--WRAPPER-->
  
  <article id="post-{id}" class="blog-list {@|item-classes}  {.repeated section tags}t_{@|slugify} {.end}  {.repeated section categories}c_{@|slugify} {.end}" data-item-id="{id}">
    
   <!--POST TILE-->
    <h1 class="title" data-content-field="title">
      {.passthrough?}
       <!--  <a href="{sourceUrl}" target="_blank">{title}</a>  -->
      {.or}
        <a href="{fullUrl}">{title}</a>
      {.end}
    </h1>
<!--CATEGORIES-->

    {.repeated section categories}
      <a class="category" href="?category={@|url-encode}">{@}</a>{.alternates with} 
    {.end}

    <!--TAGS-->

    {.repeated section tags}
      <a class="tag" href="?tag={@|url-encode}">{@}</a>{.alternates with} 
    {.end}
        <!--POST META-->
    
      <!--AUTHOR AND DATE-->
    
    <p class="meta">
      <a href="{collection.fullUrl}?author={author.id}">{author.displayName}</a> <time datetime="{addedOn|date %F}">{addedOn|date %B %d, %Y}</time>
      {.section sourceUrl}
        <a href="{@}" class="source" target="_blank">Source</a>
      {.end}
    </p>    
    <!--MAIN IMAGE-->
    {.main-image?}  
      <a href="{fullUrl}" class="main-image content-fill">
        <img src="{assetUrl}?format=300w" />
      </a>
    {.end}   
    <!--EXCERPT-->
    {.if excerpt}
      <div class="article--excerpt">{excerpt}</div>
      <div class="more-link"><a class="link" href="{fullUrl}">Read More</a></div>
    {.end}

    <!--BLOG INJECTION-->

    {postItemInjectCode}    
<!--LOCATION-->
<hr>
  </article>

  {.or}
    No blog posts yet.
{.end}
  
<!--PAGINATION-->
  
{.if pagination}
  <nav>

    <!--NEWER PAGE-->

    {.if pagination.prevPage}
      <a href="{pagination.prevPageUrl}">Newer</a>
    {.or}
      <a class="disabled">Newer</a>
    {.end}

    <!--OLDER PAGE-->

    {.if pagination.nextPage}
      <a href="{pagination.nextPageUrl}">Older</a>
    {.or}
      <a class="disabled">Older</a>
    {.end}

  </nav>
{.end}
